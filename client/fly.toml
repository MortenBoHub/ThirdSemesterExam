# fly.toml app configuration file generated for projectsolutionclient on 2025-09-15T10:47:32+02:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'doededuer-jerneif'
primary_region = 'fra'

[build]
dockerfile = './Dockerfile'
  [build.args]
    # API base URL baked into the Vite build at image build time.
    # Change this to your Fly API app URL.
    VITE_API_BASE_URL = 'https://jerneif-doededuer.fly.dev'

[http_service]
internal_port = 8080
force_https = true
auto_stop_machines = 'stop'
auto_start_machines = true
min_machines_running = 0
processes = ['app']

[[vm]]
size = 'shared-cpu-1x'
